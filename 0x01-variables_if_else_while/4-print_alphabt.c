#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

char alpha[] = "abcdfghijklmnoprstuvwxyz";
int mimi;
for (mimi = 0; mimi < 24; mimi++)
putchar(alpha[mimi]);
putchar('\n');
return (0);
}
